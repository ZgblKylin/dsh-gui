# AGENTS.md

## 环境/工具说明

优先使用grep工具和ripgrep命令，都不可用时才使用grep命令。

## 行文规范

编写/维护文档、大段代码注释、编写git提交的详细信息时，需要先参考`$env:DSH_HOME/.agents/docs/行文规范.md`。

## git提交

git提交信息遵循 conventional commits 规范，详细信息参考行文规范。
