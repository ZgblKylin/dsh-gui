# dsh-remote

多后端远程连接插件（DeepSeek Harness GUI）。标题栏标签页、汉堡菜单、新建连接页、
本地后端启动 + 远端 SSH 启动 dsh 与安全隧道转发（VSCode Remote SSH 风格，不部署代码）、
以及 Docker 连接（`docker exec` 启动容器内 dsh 并桥接回环端口，**无需端口映射**）。非侵入、自托管。

详见 [docs/README.md](docs/README.md)。
